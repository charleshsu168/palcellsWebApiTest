@model WebApplication16.Models.BatteryLog

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>BatteryLog</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.BatteryID)

        <div class="form-group">
            @Html.LabelFor(model => model.Voltage, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Voltage)
                @Html.ValidationMessageFor(model => model.Voltage)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Current, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Current)
                @Html.ValidationMessageFor(model => model.Current)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
